package DispoSell.repositories;

import DispoSell.models.ShipperDelivery;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ShipperDeliveryRepository extends JpaRepository<ShipperDelivery, Long> {
}
